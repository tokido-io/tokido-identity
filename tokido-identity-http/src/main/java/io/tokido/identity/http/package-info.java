/**
 * Transport-neutral HTTP protocol layer. Maps a buffered, immutable
 * {@code HttpRequest} to engine calls and serializes the {@code HttpResponse}.
 * No framework; the substrate every runtime adapter reuses.
 */
package io.tokido.identity.http;
