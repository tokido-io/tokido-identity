# Parked modules (not in the build)

- `mfa/` — the working TOTP/recovery library, to be reworked into
  `tokido-identity-plugin-mfa` post-v1. Do not publish from here.
- `identity-scaffolding/` — the stalled Duende-mirror SPIs/value types, kept as
  raw material (Approach B). Port value types from here as needed.

Nothing here is in the Maven reactor.
